# 文档命名规则（快速参考）

文档文件名必须使用以下结构：`[可选大写分类]-[数字编号]-[文档名称].md`

**推荐校验正则**：`^(?:[A-Z]+-)?[0-9]+-[A-Za-z0-9\u4e00-\u9fa5_]+\.md$`

**详细规范、编号递增要求、完整示例等，请查阅 skill：`document-naming-convention`**

## 执行优先级

当本规则与其他命名建议冲突时，本规则优先。
